[pytest]
markers =
    integration: hits live Bradbury over the network; slow and opt-in

# Integration tests are excluded by default so `pytest` stays fast and offline.
# They are the only tests that can prove chain-layer value movement, so run them
# before any deployment:
#
#     pytest tests/integration -m integration
addopts = -m "not integration"
